/* ============================================================================
 * 蓝色大肥鱼 · 深海皮肤 —— 宿主半侧
 *
 * 这一半只负责“占一行”：Row 存在 → client-modules 才扫描得到本包的
 * dsh.client 声明 → 浏览器才拿得到 ./client 那份 bundle。
 * 皮肤本身全部在浏览器里跑（lib/client.js，由 plugin/build.mjs 生成）。
 *
 * 这里刻意不声明 inject：宿主半侧不消费任何服务，少一个依赖就少一种
 * “entry 没激活 → 整个 web 启动报错”的可能。
 * ========================================================================== */

/** Cordis 插件名（与包名一致，浏览器侧模块 id 也用它）。 */
export const name = 'dsh-blue-fat-fish'

/** 装载时留一行日志，方便在 dsh 日志里确认这一行真的进来了。 */
export function apply(ctx) {
  ctx.logger?.info?.('蓝色大肥鱼皮肤：宿主半侧已装配，浏览器半侧由 client-modules 提供')
}
